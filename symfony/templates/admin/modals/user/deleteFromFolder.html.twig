{% form_theme userDeleteForm 'bootstrap_4_horizontal_layout.html.twig' %}
<div class="modal" id="userDeleteFromFolderModal_{{ user.id }}">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title">{{ 'admin.form.folder.user.delete.title'|trans }}</h3>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                <div class="modal-message">
                    {{ 'admin.form.user.delete.message'|trans }}
                </div>
                <div class="modal-action mt-3">
                    {{ form_start(userDeleteForm, {action: path('user_removeFromFolder', {folder: folder.id, user: user.id}), method: "POST"}) }}
                    {{ form_widget(userDeleteForm.submit) }}
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ 'admin.modal.cancel'|trans }}</button>
                    {{ form_end(userDeleteForm) }}
                </div>
            </div>
        </div>
    </div>
</div>
