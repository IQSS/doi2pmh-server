<nav class="navbar sticky-top navbar-dark bg-dark navbar-expand-lg">
  <div class="container">
  <a class="navbar-brand" href="#">DOI2PMH</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link{% if current is defined and current == "doi" %} active{% endif %}" href="{% if current is defined and current == "doi" %}#{% else %}{{ path('admin_get_folder') }}{% endif%}">{% trans %}DOIs{% endtrans%}{% if current is defined and current=="doi" %} <span class="sr-only">(current)</span>{% endif %}</a>
      </li>
      {% if is_granted('ROLE_ADMIN') %}
      <li class="nav-item">
        <a class="nav-link{% if current is defined and current=="user" %} active{% endif %}" href="{% if current is defined and current == "user" %}#{% else %}{{ path('admin_user') }}{% endif%}">{% trans %}Users{% endtrans %}{% if current is defined and current=="user" %} <span class="sr-only">(current)</span>{% endif %}</a>
      </li>
      {% endif %}
    </ul>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="localeDropdownMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-globe"></i> {% trans %}language{% endtrans%}
        </a>
        <div class="dropdown-menu" aria-labelledby="localeDropdownMenu">
          <a class="dropdown-item" href="{{ path(app.request.get('_route'), {"_locale": "en"}) }}">🇺🇸 English</a>
          <a class="dropdown-item" href="{{ path(app.request.get('_route'), {"_locale": "fr"}) }}">🇫🇷 Français</a>
        </div>
      </li>
      {% if is_granted('ROLE_USER') %}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="localloginDropdownMenu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <i class="fas fa-user"></i> {{ app.user.email }}
        </a>
        <div class="dropdown-menu" aria-labelledby="localloginDropdownMenu">
          <a class="dropdown-item" href="{{ path('local_logout') }}"><i class="fas fa-sign-out-alt"></i> {% trans %}Logout{% endtrans %}</a>
        </div>
      </li>
      {% endif %}
    </ul>
  </div>
  </div>
</nav>